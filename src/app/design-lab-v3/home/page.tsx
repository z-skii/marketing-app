import V2UserHome from "../../design-lab-v2/home/page";

/** The V2 Personal Home, unchanged in intent, reachable inside V3. Personal mode carries no Loyalty member identities. Its internal preview links remain the V2 Lab's. */
export default function V3UserHome() { return <V2UserHome />; }
