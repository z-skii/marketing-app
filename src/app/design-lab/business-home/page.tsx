import { BusinessHomeScreen } from "./Screen";

/** /design-lab/business-home: the coded prototype, rendered as a page. The screen itself lives in Screen.tsx so the public page can embed it live. */
export default function Page() {
  return <BusinessHomeScreen />;
}
